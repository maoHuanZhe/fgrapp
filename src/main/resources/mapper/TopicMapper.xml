<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fgrapp.dao.TopicMapper">
    <delete id="remove">
        DELETE
            ft,fbc
        FROM
            func_topic ft
                LEFT JOIN func_context_class fbc ON ft.id  = fbc.contextId
        WHERE
            ft.id = #{id}
    </delete>
    <select id="getPage" resultType="java.util.Map">
        select
        id, problem, answer, summary, classId, lastUpdateTime
        from func_topic t
        <where>
            <if test="ew.problem != null and ew.problem != ''">
                and problem like concat('%', #{ew.problem}, '%')
            </if>
            <if test="ew.classId != null and ew.classId != ''">
                and classId = #{ ew.classId }
            </if>
        </where>
        order by lastUpdateTime desc
    </select>
    <select id="getList" resultType="com.fgrapp.domain.FuncTopicDo">
        select
            id, problem, classId, lastUpdateTime
        from func_topic
        order by lastUpdateTime desc
    </select>
</mapper>